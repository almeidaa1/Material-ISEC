#include <stdio.h>
float main()

{
    float n;

    printf("Numero:");
    scanf("%f",&n);

    if(n)
        if(n>0)
            printf("POSITIVO");
        else
            printf("NEGATIVO");
return 0;
}
